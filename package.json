{
  "name": "whale-network",
  "version": "1.0.0",
  "description": "A cryptocurrency Project",
  "main": "truffle-config.js",
  "directories": {
    "test": "test"
  },
  "scripts": {
    "deploy-test": "truffle migrate --network=bscTestnet --reset",
    "deploy-main": "truffle migrate --network=bscMainnet --reset",
    "compile": "truffle compile",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Shivam Singh",
  "license": "MIT",
  "dependencies": {
    "@truffle/hdwallet-provider": "^1.5.0"
  },
  "devDependencies": {
    "@openzeppelin/contracts": "^4.4.1",
    "solhint": "^3.3.6",
    "truffle": "^5.4.27"
  }
}
